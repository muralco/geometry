/**
 * Number of decimal places to round to by default.
 */
export const DEFAULT_PRECISION = 2;

/**
 * A small value used for floating-point comparisons.
 * This is useful to avoid issues with floating-point precision when comparing values.
 */
export const EPSILON = 1e-5;
